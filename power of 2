/* Точная степень двойки */

#include <bits/stdc++.h>

using namespace std;

bool two(double n){
	if(n == 1) return 1;
	else if(n > 1 && n < 2) return 0;
	return two(n/2);
}

int main(){
	double n;
	cin >> n;
	if(two(n) == 1) cout << "YES";
	else cout << "NO";
	return 0;
}
